# agent-backend/requirements.txt
# Python 3.12+ required (3.13 compatible)

# Core framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Strands Agents SDK + MCP transport
strands-agents==0.1.9
strands-agents-tools==0.1.9
mcp==1.3.0

# AWS / Bedrock
boto3==1.35.98

# Database (via MCP subprocess — not used directly by agent-backend)
# asyncpg is only in mcp-server/requirements.txt

# Testing
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-mock==3.15.1
httpx==0.28.1           # required by FastAPI TestClient async mode

# Observability / logging (structured JSON logs for CloudWatch)
python-json-logger==3.2.1
